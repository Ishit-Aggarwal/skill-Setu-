"use client";

/**
 * Sitting a test now happens in the secure exam room (ExamRoom.jsx): the
 * same props as before, so every card and attempt log that opened this
 * dialog opens the room instead.
 */
export { default } from "./ExamRoom";
